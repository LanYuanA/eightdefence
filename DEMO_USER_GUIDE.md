# 双解耦演示操作手册

这份手册面向第一次接触本项目的演示人员。按顺序操作即可完成两段演示：先演示“软硬解耦”，再演示“软软解耦”。

## 一、演示前准备

### 1. 硬件和软件

- 一台安装 Ubuntu 或其他 Linux 系统的电脑。
- 本项目代码，默认目录为 `/home/kylin/ylz/work/eightdefence`。
- 一个通过 RS485 接入的电机驱动器。系统当前按“一次只接一台真实电机”工作。
- 串口设备默认为 `/dev/ttyS9`，波特率默认为 `9600`。
- 电机必须固定牢靠，首次操作建议空载。确认附近人员和线缆不会接触旋转部件。

三个真实电机编号与通信地址如下：

| 页面编号 | 通信地址 | 页面名称 |
| --- | --- | --- |
| 电机1 | `0x02` | 电机1 |
| 电机2 | `0x0E` | 电机2 |
| 电机3 | `0x0F` | 电机3 |

### 2. 第一次使用时安装工具

打开“终端”，逐行执行：

```bash
sudo apt update
sudo apt install -y build-essential make nodejs npm
```

检查工具是否可用：

```bash
g++ --version
make --version
node --version
npm --version
```

每条命令都能显示版本号即可。

### 3. 第一次使用或代码更新后编译

```bash
cd /home/kylin/ylz/work/eightdefence
cd ui
npm ci
cd ..
make all
```

编译完成后，项目目录中应有 `app_gateway` 文件。以后如果代码没有更新，可以直接启动，无需重复安装依赖和编译。

## 二、每次演示前启动系统

### 1. 检查串口

```bash
ls -l /dev/ttyS9
```

能看到 `/dev/ttyS9` 表示系统已经识别串口。然后确认电机驱动器已上电，RS485 的 A/B 线没有接反。

### 2. 启动网关

```bash
cd /home/kylin/ylz/work/eightdefence
sudo ./app_gateway -d /dev/ttyS9
```

看到串口打开、HTTP 服务启动一类信息后，不要关闭这个终端。网关运行期间必须让它保持打开。

如果实际串口不是 `/dev/ttyS9`，把命令最后的路径换成实际路径，例如：

```bash
sudo ./app_gateway -d /dev/ttyUSB0
```

### 3. 打开演示页面

打开 Chrome 或 Edge，在地址栏输入：

```text
http://localhost:8080/hardware-decoupling
```

如果页面曾经打开过旧版本，按 `Ctrl+Shift+R` 强制刷新。

## 三、先演示软硬解耦

软硬解耦页面用于说明：应用和控制服务保持不变，系统可以发现当前接入的电机、切换绑定，并通过同一组抽象接口控制不同设备。

### 1. 等待识别真实电机

进入页面后等待数秒。系统会按顺序扫描三个地址，识别到一台电机后只向这台电机发送心跳查询。

识别成功时应看到：

- 当前电机卡片亮起并显示“在线”。
- 另外两台电机保持暗色并显示“离线”。
- 设备抽象层的控制接口亮起。
- 紫色动态效果表示状态查询，青色动态效果表示控制指令下发。

如果三台电机一直都是暗色，请先查看本手册末尾的“常见问题”。

### 2. 演示启动、调速、转向和停止

1. 在页面左下角选择亮起的在线电机。
2. 输入目标转速，例如 `200` RPM。
3. 选择“正转”或“反转”。
4. 点击运行参数右侧的“启动电机”。
5. 观察抽象接口、中间件、目标电机之间的指令流动，并查看实时转速逐步变化。
6. 点击“停止电机”，确认实时转速回到 `0 RPM`。

真实电机存在加速过程，实时转速不一定在点击后立即等于目标转速。

### 3. 演示设备故障和替换

1. 先让当前在线电机正常运行。
2. 点击“模拟故障”，当前电机会停止，页面进入保护状态。
3. 断开当前电机，接入另一台地址不同的电机。
4. 等待新电机卡片亮起。系统发现原绑定设备离线后，会重新扫描三个地址。
5. 点击新亮起的电机卡片。
6. 点击“确认切换”。
7. 观察逻辑绑定更新、控制指令经过中间件到达新电机。页面会显示新电机已接管，而上层应用和控制服务保持不变。

需要重新开始这段演示时，点击“重置演示”。系统会把当前在线电机设为绑定设备，并以 `200 RPM` 正转初始化。

## 四、再演示软软解耦

### 1. 切换页面

点击页面最右下角的小型切换按钮，或直接在地址栏输入：

```text
http://localhost:8080/software-defined-platform
```

软软解耦页面用于说明：应用只描述任务目标，AI 决策结果作为输入交给“电机群控模块”，群控模块输出统一的 `stop` 或维持指令，再由设备抽象层映射到真实电机和虚拟电机。

### 2. 三种演示任务

系统只读取输入内容的前三个字，冒号后的场景说明可以自由填写。

| 输入开头 | 关闭的电机 | 保持不变的电机 |
| --- | --- | --- |
| `任务一` | 1、2、5 | 3、4、6、7、8 |
| `任务二` | 2、3、8 | 1、4、5、6、7 |
| `任务三` | 4、5、6 | 1、2、3、7、8 |

例如可以输入：

```text
任务一：机舱局部过热，需要关闭相关电机
```

电机1、2、3标记为真实设备，电机4至8为前端虚拟设备。由于一次只接入一台真实电机，后端只会控制当前在线且被本任务选中的真实电机，其余变化用于界面演示：

- 当前接入电机1时，使用“任务一”可以看到真实电机停止。
- 当前接入电机2时，使用“任务一”或“任务二”可以看到真实电机停止。
- 当前接入电机3时，使用“任务二”可以看到真实电机停止。
- “任务三”只影响虚拟电机，适合演示纯软件映射效果。

### 3. 完整操作步骤

1. 点击“重置设备状态”。当前在线的真实电机会恢复为 `200 RPM` 正转，页面中的八台电机恢复为“开”。
2. 在顶部任务输入框输入以“任务一”“任务二”或“任务三”开头的内容。
3. 点击“AI生成方案”。
4. 观察右侧 03 层逐项出现分析结果，电机群控模块接收结果并输出控制动作。
5. 观察 03 层到 02 层的数据流：红色表示关闭，绿色表示保持正常。
6. 点击“人工确认执行”。
7. 观察一组粒子从上层缓慢游动到下层，表示控制数据已经下发。
8. 观察目标电机变暗并显示“关”，其余电机继续显示“开”。如果当前在线真实电机属于目标集合，它也会实际停止。

要开始下一次演示，先点击“重置设备状态”，再输入新任务并重新生成方案。重置后旧的 AI 方案和设备抽象映射会被清空。

如果需要立即结束演示，点击“终止演示”。该按钮会停止当前在线的真实电机，并清空本轮 AI 方案、群控输出和设备映射结果。

## 五、演示结束

1. 在页面中点击“终止演示”，确认真实电机停止。
2. 回到运行网关的终端，按 `Ctrl+C`。
3. 确认终端重新出现命令提示符后，再关闭电机驱动器电源。

## 六、常见问题

### 浏览器显示“无法访问此站点”或 `ERR_CONNECTION_RESET`

网关通常没有运行，或已经退出。回到终端重新执行：

```bash
cd /home/kylin/ylz/work/eightdefence
sudo ./app_gateway -d /dev/ttyS9
```

同一时间只启动一个网关，避免多个程序争用 8080 端口和串口。

### 提示串口不存在

执行：

```bash
ls -l /dev/ttyS9
ls -l /dev/ttyUSB*
```

找到实际串口后，用 `-d` 指定它。若两个命令都找不到设备，请检查 USB/串口连接和驱动。

### 提示没有权限访问串口

本手册的启动命令使用 `sudo`，一般可以直接运行。也可以把当前用户加入串口用户组：

```bash
sudo usermod -aG dialout "$USER"
```

执行后需要注销并重新登录才生效。

### 三台电机都显示离线

依次检查：

1. 驱动器是否上电。
2. RS485 A/B 线是否接反。
3. 串口路径和波特率是否正确。
4. 接入电机的地址是否为 `0x02`、`0x0E` 或 `0x0F`。
5. 等待一个完整扫描周期后再判断；换电机后系统需要先确认旧设备离线，再重新扫描。

### 页面还是旧样式

先按 `Ctrl+Shift+R`。如果仍然是旧页面，停止网关后重新编译并启动：

```bash
cd /home/kylin/ylz/work/eightdefence
make all
sudo ./app_gateway -d /dev/ttyS9
```

### 点击控制按钮提示失败

确认页面中至少有一台电机亮起并显示在线。真实模式下，后端不会向离线地址发送控制指令；虚拟电机只改变前端展示状态。
