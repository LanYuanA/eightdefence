# 应用独立日志与安防传感器离线检测改造记录

## 1. 改造概述

本次改造完成两项功能：

1. **应用独立日志系统** — 每个应用拥有独立的日志文件，主日志仅保留系统级日志
2. **安防传感器离线检测** — 设备未接入时正确显示离线状态，不再误报"一切正常"

## 2. 应用独立日志系统

### 2.1 需求背景

原有系统只有一个全局 Logger（单例），所有日志写入 `logs/gateway_YYYYMMDD_HHMMSS.log`。用户希望：
- 主日志保留系统级日志
- 每个应用有独立的日志文件
- 应用日志放在 `logs/{appName}/` 子目录下

### 2.2 日志目录结构

```
logs/
├── gateway_20260526_100507.log    # 主日志 (仅系统级事件)
├── security/                      # 安防应用专属目录
│   ├── security_20260526_100507.log
│   └── security_20260526_100054.log
├── fire/                          # 消防应用目录 (未来)
│   └── fire_*.log
└── ...
```

### 2.3 新增文件

#### `core/app_logger.hpp` — 应用日志器类

```cpp
class AppLogger {
public:
    AppLogger(const std::string& appName, const std::string& logDir = "./logs");
    ~AppLogger();
    int init();
    void shutdown();
    void log(LogLevel level, const char *file, int line, const char *fmt, ...);
    void logV(LogLevel level, const char *file, int line, const char *fmt, va_list args);
private:
    std::string appName_;
    std::string logDir_;
    std::string appDir_;         // logs/{appName}/
    std::string currentLogFile_;
    std::ofstream fileStream_;
    std::mutex mtx_;
    bool initialized_ = false;
};
```

**便捷宏**（在 AppBase 子类中使用）：

```cpp
APP_LOG_INFO("初始化完成, 路由: %s", m_routePrefix.c_str());
APP_LOG_WARNING("设备离线: %s", name);
APP_LOG_ERROR("通信失败: %s", err);
```

#### `core/app_logger.cpp` — AppLogger 实现

- `init()`: 创建 `logs/{appName}/` 子目录，创建 `{appName}_YYYYMMDD_HHMMSS.log` 文件
- `logV()`: 仅写入应用专属日志文件（不写入主系统日志）
- 日志格式与主日志一致：`YYYY-MM-DD HH:MM:SS.mmm [LEVEL] file:line - message`

#### `application/app_base.cpp` — AppBase 实现

```cpp
AppBase::~AppBase() {
    if (m_logger) { delete m_logger; m_logger = nullptr; }
}

int AppBase::initLogger(const std::string& logDir) {
    if (m_logger) return 0;
    m_logger = new AppLogger(m_name, logDir);
    return m_logger->init();
}
```

### 2.4 修改的文件

| 文件 | 修改内容 |
|------|---------|
| `application/app_base.hpp` | 添加 `AppLogger* m_logger` 成员，析构函数改为 cpp 中实现 |
| `application/app_manager.hpp` | `initAll()` 中在 `app->init()` 前调用 `app->initLogger()` |
| `application/apps/security/app_security.cpp` | `LOG_INFO` 改为 `APP_LOG_INFO`，告警事件添加 `APP_LOG_WARNING` |
| `application/web_server.cpp` | 日志列表API支持扫描子目录 |
| `Makefile` | 添加 `core/app_logger.o` 和 `application/app_base.o` |

### 2.5 Web API 变更

#### `GET /api/logs/list`

返回结果现在包含子目录路径：

```json
[
    "security/security_20260526_100507.log",
    "gateway_20260526_100507.log",
    "gateway_20260526_100054.log"
]
```

#### `GET /api/logs/{path}`

支持子目录路径，如 `GET /api/logs/security/security_20260526_100507.log`。

安全限制：路径中不能包含 `..`（防止路径遍历）。

### 2.6 使用方式

在应用子类中直接使用宏：

```cpp
// app_security.cpp
int AppSecurity::init() {
    APP_LOG_INFO("初始化...");
    return 0;
}

void AppSecurity::triggerAlarm(...) {
    APP_LOG_WARNING("水浸风险警报: 检测到水位异常(5.8cm)");
    addLog("alarm", "水浸风险警报", "...");  // 前端API用
}
```

## 3. 安防传感器离线检测

### 3.1 问题描述

设备未接入时，安防应用的状态API返回：

```json
{
    "system": { "overallRisk": "低风险", "systemNormal": true },
    "water": { "risk": "低风险" },
    "intrusion": { "risk": "低风险" }
}
```

实际应该是"设备离线"状态，而非"一切正常"。

### 3.2 根因分析

**问题A — 传感器默认值与"安全"值无法区分：**

设备读取失败时，驱动函数直接 `return`，不更新状态变量。状态变量保持初始值 `0`，而 `0` 恰好是"无报警/正常/安全"的含义。

**问题B — 风险计算不检查设备在线状态：**

```cpp
SecurityRiskLevel calcWaterRisk(float level, int sensorState) {
    if (sensorState != 0 || level > 3.0f) return HIGH;
    if (level > 1.0f) return MEDIUM;
    return LOW;   // sensorState==0 时命中这里
}
```

`DeviceStatusCpp` 虽然跟踪了 `online` 状态，但风险计算完全忽略了它。

### 3.3 修复方案

修改 `application/apps/security/app_security.cpp`：

#### 3.3.1 `evaluateRisk()` 增加设备在线检查

```cpp
void AppSecurity::evaluateRisk(int waterState, int irState, int radarState) {
    bool waterOnline = dev_water.isOnline();
    bool irOnline = dev_infrared.isOnline();

    m_state.waterRisk = calcWaterRisk(m_state.waterLevel.load(), waterState);
    m_state.intrusionRisk = calcIntrusionRisk(irState, radarState);
    m_state.gasRisk = calcGasRisk(m_state.gasConcentration.load());

    // 设备离线时, 对应子系统标记为中风险
    if (!waterOnline) m_state.waterRisk = SecurityRiskLevel::MEDIUM;
    if (!irOnline) m_state.intrusionRisk = SecurityRiskLevel::MEDIUM;

    int maxRisk = std::max({...});
    m_state.overallRisk = static_cast<SecurityRiskLevel>(maxRisk);

    // 设备离线或有风险时, 系统不正常
    bool allOnline = waterOnline && irOnline;
    m_state.systemNormal.store(maxRisk == 0 && allOnline);
}
```

#### 3.3.2 API 响应增加 `online` 字段

`handleGetStatus()` 和 `handleGetSensors()` 返回的 JSON 增加 `online` 字段：

```json
{
    "system": { "overallRisk": "中风险", "systemNormal": false },
    "water": { "online": false, "risk": "中风险" },
    "intrusion": { "online": false, "risk": "中风险" }
}
```

前端可根据 `online` 字段显示"设备离线"提示。

### 3.4 修复效果

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 设备全部离线 | 低风险 / 系统正常 | 中风险 / 系统异常 |
| 水浸传感器离线 | 低风险 | 中风险 / water.online=false |
| 红外探测器离线 | 低风险 | 中风险 / intrusion.online=false |
| 设备在线 + 无报警 | 低风险 / 系统正常 | 低风险 / 系统正常 |

## 4. 设备手动控制 API

### 4.1 需求背景

用户需要在安防页面手动开启/关闭设备（水浸传感器、红外探测器），以便测试离线检测功能或临时禁用设备。

### 4.2 设备控制接口

#### `GET /api/device/control?device={name}&action={enable|disable}`

| 参数 | 说明 |
|------|------|
| `device` | 设备名称：`water`（水浸传感器）、`infrared`（红外探测器） |
| `action` | `enable` 启用设备 / `disable` 禁用设备 |

响应：
```json
{ "ok": true, "device": "water", "action": "disable" }
```

### 4.3 底层实现

#### `DeviceStatusCpp::forceOffline()` — 新增方法

```cpp
void forceOffline() {
    std::lock_guard<std::mutex> lock(mtx);
    online = 0;
}
```

#### `DevWater::setOnline()` / `DevInfrared::setOnline()` — 新增方法

```cpp
void setOnline(bool online) {
    if (online) status_.onSuccess();
    else status_.forceOffline();
}
```

调用 `setOnline(true)` 将设备标记为在线并重置失败计数，`setOnline(false)` 强制标记为离线。

### 4.4 web_server.cpp 路由

```cpp
} else if (req.find("GET /api/device/control") == 0) {
    std::string device = get_query_param(req, "device");
    std::string action = get_query_param(req, "action");
    bool ok = false;
    if (device == "water") {
        if (action == "enable") { dev_water.setOnline(true); ok = true; }
        else if (action == "disable") { dev_water.setOnline(false); ok = true; }
    } else if (device == "infrared") {
        if (action == "enable") { dev_infrared.setOnline(true); ok = true; }
        else if (action == "disable") { dev_infrared.setOnline(false); ok = true; }
    }
    // 返回 JSON 响应
}
```

## 5. 安防前端页面改造

### 5.1 新增导航栏

安防应用页面顶部新增导航栏，包含：
- **返回主菜单** 按钮 — 链接到 `/` 主页
- **系统状态指示器** — 实时显示系统正常/异常
- **时钟显示**

### 5.2 API 轮询

前端每 2 秒调用 `GET /security/api/status` 获取实时数据，动态更新：
- 系统整体状态卡片（正常/异常/风险等级）
- 报警服务状态
- 防护处置状态
- 各传感器数据和风险等级
- 设备在线/离线状态（离线时显示红色 `[设备离线]` 标记）
- 硬件层设备在线状态（离线设备红色高亮）

### 5.3 模拟按钮改为真实 API 调用

原前端模拟按钮是本地 JavaScript 变量修改，现改为真实调用后端 API：
- `模拟水浸异常` → `GET /security/api/control?action=simulate&target=water`
- `恢复正常` → `GET /security/api/control?action=reset&target=water`
- 入侵和气体同理

### 5.4 日志列表排序修复

**问题**: `security/security_*.log` 按字母排序排在 `gateway_*.log` 前面，导致日志页面实时标签显示在安防日志上。

**修复**: `web_server.cpp` 中日志列表排序改为 gateway 日志优先：

```cpp
std::sort(files.begin(), files.end(), [](const std::string& a, const std::string& b) {
    bool aGw = (a.find("gateway") == 0);
    bool bGw = (b.find("gateway") == 0);
    if (aGw != bGw) return aGw > bGw;  // gateway 排前面
    return a > b;
});
```

## 6. Vue 前端改造

### 6.1 Security.vue — 安防页面

#### 设备在线/离线状态显示

`SecurityStatus` 接口新增 `online` 字段：

```typescript
interface SecurityStatus {
    water: { level: number; state: number; risk: string; online: boolean }
    intrusion: { irState: number; radarState: number; risk: string; online: boolean }
    // ...
}
```

传感器数据行中根据 `online` 字段显示 `[设备离线]` 红色标记或 `[在线]` 绿色标记。

#### 设备手动控制

每个传感器行末尾添加启用/禁用按钮：

```typescript
const toggleDevice = async (device: string, currentOnline?: boolean) => {
    const action = currentOnline === false ? 'enable' : 'disable'
    const res = await axios.get("/api/device/control", { params: { device, action } })
    if (res.data.ok) fetchStatus()
}
```

按钮样式：
- `.btn-enable` — 绿色边框，点击启用设备
- `.btn-disable` — 红色边框，点击禁用设备

### 6.2 Dashboard.vue — 主控台

#### 安防状态获取

新增 `securityStatus` ref 和 `fetchSecurityStatus()` 函数，每 2 秒轮询：

```typescript
const securityStatus = ref<any>({ water: { online: true }, intrusion: { online: true } })

const fetchSecurityStatus = async () => {
    const res = await axios.get("/security/api/status")
    securityStatus.value = res.data
}
```

#### 设备在线/离线指示

报警区域根据 `securityStatus` 显示设备状态：
- 在线：绿色圆点
- 离线：灰色圆点 + "离线" 文字

新增 CSS：
```css
.dot-gray { background: #aaa; }
.status-offline { color: #aaa; font-size: 12px; }
```

## 7. 编译与测试

### 7.1 编译

```bash
make clean && make           # 后端
cd ui && npm run build       # 前端
```

### 7.2 测试日志分离

```bash
./app_gateway &
ls logs/security/
# 应看到 security_*.log 文件
```

### 7.3 测试传感器离线检测

```bash
curl -s http://localhost:8080/security/api/status | python3 -m json.tool
# 应返回 systemNormal: false, water.online: false, intrusion.online: false
```

### 7.4 测试设备控制 API

```bash
# 禁用水浸传感器
curl -s "http://localhost:8080/api/device/control?device=water&action=disable"
# {"ok":true,"device":"water","action":"disable"}

# 启用水浸传感器
curl -s "http://localhost:8080/api/device/control?device=water&action=enable"
# {"ok":true,"device":"water","action":"enable"}
```

## 8. 有害气体安全防护 — 接入云测仪真实传感器

### 8.1 需求背景

原有有害气体模块使用模拟的 `gasConcentration` 单一数值。云测仪（DevCloud*）已有 TVOC、CH2O、O3、CO2 四种有害气体传感器，需要接入真实数据，超出安全阈值时触发报警。

### 8.2 传感器与阈值

| 传感器 | 全局变量 | 单位 | 中风险阈值 | 高风险阈值 |
|--------|---------|------|-----------|-----------|
| TVOC (挥发性有机物) | `dev_tvoc` | ppb | >200 | >500 |
| CH2O (甲醛) | `dev_ch2o` | ppb | >50 | >100 |
| O3 (臭氧) | `dev_o3` | ppb | >60 | >100 |
| CO2 (二氧化碳) | `dev_co2` | ppm | >800 | >1000 |

阈值依据：GB/T 18883 室内空气质量标准。

### 8.3 后端改动

#### `app_security.hpp`

- SecurityState 中移除 `gasConcentration`，新增 `gasSimulated` 标志和模拟值缓存
- `calcGasRisk(int)` 替换为 `calcSingleGasRisk(int, int, int)` 通用版本

#### `app_security.cpp`

**`handleGetStatus()`** — 读取四个真实传感器值，返回结构化 JSON：

```json
"gas": {
  "online": true,
  "tvoc": { "value": 120, "unit": "ppb", "online": true },
  "ch2o": { "value": 30, "unit": "ppb", "online": true },
  "o3": { "value": 45, "unit": "ppb", "online": true },
  "co2": { "value": 450, "unit": "ppm", "online": true },
  "risk": "低风险",
  "ventilationActive": false
}
```

**`evaluateRisk()`** — 气体风险 = 所有在线传感器中的最高风险。全部离线时标记为中风险。

**模拟机制** — `gasSimulated` 为 true 时使用 state 中的模拟值，否则读取真实传感器。模拟时设置所有传感器为高风险值，重置时恢复为读取真实数据。

### 8.4 前端改动

#### Security.vue

有害气体卡片改为显示四个传感器实时读数：

```
有害气体探测器        [在线/离线]
├── TVOC (挥发性有机物)   120 ppb
├── CH2O (甲醛)           30 ppb
├── O3 (臭氧)             45 ppb
├── CO2 (二氧化碳)        450 ppm
├── 风险评估              低风险
└── [模拟气体泄漏] [恢复正常]
```

- 各传感器离线时显示 `--`（灰色未知状态）
- 超限时数值显示红色
- 基础硬件层中有害气体探测器显示在线/离线状态

### 8.5 编译与测试

```bash
make                           # 后端
cd ui && npm run build         # 前端
# 拷贝到 public
cp ui/dist/assets/* public/assets/
cp ui/dist/index.html dashboard.html
```

测试：
1. 访问安防页面，检查有害气体卡片显示四个传感器读数
2. 点击"模拟气体泄漏"，各传感器显示超限值，风险变高风险
3. 检查日志中记录了气体超限告警
4. 点击"恢复正常"，恢复读取真实传感器数据

## 9. 原子服务层

### 9.1 需求背景

安防应用需要调用声光报警、排水、温湿度调控、有害气体处理等能力，这些能力可能被其他应用（如消防、环境监控）复用。因此将设备控制逻辑封装为独立的原子服务层，与应用解耦。

### 9.2 架构

```
应用层 (AppSecurity, AppFire, ...)
    ↓ 调用
原子服务层 (SvcSoundLightAlarm, SvcDrainage, ...)
    ↓ 调用
设备层 (DevAlarmDevice, DevAirConditioner, ...)
    ↓ 调用
通信层 (ModbusService → SerialBus → RS-485)
```

### 9.3 服务清单

| 服务 | 类名 | 功能 | 依赖设备 |
|------|------|------|---------|
| 声光报警 | `SvcSoundLightAlarm` | 驱动报警器发出声光警报 | `dev_alarm` |
| 排水控制 | `SvcDrainage` | 模拟排水泵/阀门控制 | 无（模拟） |
| 温湿度调控 | `SvcTempHumidityControl` | 超阈值时开空调/除湿机 | `dev_ac`, `dev_humidifier` |
| 有害气体处理 | `SvcGasResponse` | 超标时开净化器通风 | `dev_purifier`, `dev_humidifier` |
| 指挥中心报警 | `SvcCommandCenter` | UI 告警 + 日志记录 | 无（纯软件） |

### 9.4 服务基类

```cpp
class SvcBase {
public:
    virtual ~SvcBase() = default;
    virtual std::string getName() const = 0;
    virtual void activate() = 0;
    virtual void deactivate() = 0;
    virtual bool isActive() const = 0;
};
```

### 9.5 各服务实现

#### SvcSoundLightAlarm — 声光报警

- `activate()`: 调用 `dev_alarm.setAlarmOn(*g_modbus, ...)` 发送 Modbus FC 0x05 写线圈命令
- `deactivate()`: 调用 `dev_alarm.setAlarmOff(...)`

#### SvcDrainage — 排水控制（模拟）

- `activate()`: 记录日志 "排水泵启动指令已发送 (模拟)"
- `deactivate()`: 记录日志 "排水泵停止指令已发送"
- 未来接入电机时替换为真实 Modbus 命令

#### SvcTempHumidityControl — 温湿度调控

- `setThresholds(tempHigh, tempLow, humHigh, humLow)` — 设置阈值
- `checkAndControl(temperature, humidity)` — 自动控制:
  - 温度 > tempHigh: 开空调制冷 (`dev_ac.setCoolOn`)
  - 温度 < tempLow: 开空调制热 (`dev_ac.setHeatOn`)
  - 湿度 > humHigh: 开除湿 (`dev_humidifier.setDehumidify`)
  - 湿度 < humLow: 开加湿 (`dev_humidifier.setHumidify`)
  - 正常范围: 关闭对应设备

#### SvcGasResponse — 有害气体处理

- `setThresholds(GasThresholds)` — 设置各气体阈值
- `checkAndControl(tvoc, ch2o, o3, co2)` — 自动控制:
  - 任一超标: 开净化器 (`dev_purifier.setPower`) + 净化模式 (`dev_humidifier.setPurify`)
  - 全部恢复: 关闭净化设备

#### SvcCommandCenter — 指挥中心报警（纯软件）

- `alert(level, title, message)` — 记录告警，写入 Logger
- `clearAlerts()` — 清除所有告警
- `getAlertsJson()` — 返回 JSON 格式告警列表，供前端展示

### 9.6 安防应用集成

AppSecurity 通过 `setServices()` 注入服务指针。`evaluateRisk()` 每次计算风险后调用 `handleRiskResponse()`:

- 水浸高风险 → 激活声光报警 + 排水 + 指挥中心告警
- 入侵高风险 → 激活声光报警 + 指挥中心告警
- 气体高风险 → 激活声光报警 + 净化器 + 指挥中心告警
- 风险解除 → 停止对应服务

### 9.7 新增文件

```
service/atomic/
├── svc_base.hpp                       — 服务基类
├── svc_sound_light_alarm.hpp/cpp      — 声光报警
├── svc_drainage.hpp/cpp               — 排水控制（模拟）
├── svc_temp_humidity_control.hpp/cpp  — 温湿度调控
├── svc_gas_response.hpp/cpp           — 有害气体处理
└── svc_command_center.hpp/cpp         — 指挥中心报警
```

### 9.8 修改文件

| 文件 | 修改内容 |
|------|---------|
| `application/apps/security/app_security.hpp` | 添加服务指针成员、`setServices()`、`handleRiskResponse()` |
| `application/apps/security/app_security.cpp` | 实现 `setServices()` 和 `handleRiskResponse()`，`evaluateRisk()` 末尾调用 `handleRiskResponse()` |
| `core/main.cpp` | 创建服务实例，注入到 AppSecurity |
| `Makefile` | 添加 5 个新的 .cpp 编译目标 |

### 9.9 验证方式

1. `make` 编译通过
2. 运行 `./app_gateway`
3. 安防页面点击"模拟水浸异常" → 日志中出现声光报警器激活、排水泵启动
4. 点击"模拟气体泄漏" → 日志中出现净化器开启
5. 点击"恢复正常" → 各服务停止，设备恢复待机
6. 主日志中出现指挥中心告警记录
